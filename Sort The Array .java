class Solution 
{ 
    int[] sortArr(int[] arr, int n) 
    { 
        // Sorting the array in ascending order
        Arrays.sort(arr);
        
        // Returning the sorted array
        return arr;
    }
}
